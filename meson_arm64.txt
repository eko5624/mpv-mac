[constants]
arch        = 'arm64'
min_version = '-mmacosx-version-min=11.0'
xcode 		= '/Applications/Xcode_16.4.app'
xctoolchain = xcode + '/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin'
sdkroot 	= xcode + '/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk'

[host_machine]
system 	   = 'darwin'
cpu_family = 'aarch64'
cpu 	   = 'arm64'
endian 	   = 'little'

[binaries]
c          = xctoolchain + '/clang'
cpp        = xctoolchain + '/clang++'
objc       = xctoolchain + '/clang'
objcpp     = xctoolchain + '/clang++'
nm         = xctoolchain + '/nm'
ar         = xctoolchain + '/ar'
strip      = xctoolchain + '/strip'
lipo       = xctoolchain + '/lipo'
strings    = xctoolchain + '/strings'
ranlib 	   = xctoolchain + '/ranlib'
pkg-config = 'pkg-config'

[built-in options]
c_args           = ['-arch', arch, '-isysroot', sdkroot, min_version]
cpp_args         = ['-arch', arch, '-isysroot', sdkroot, min_version, '-stdlib=libc++']
objc_args        = ['-arch', arch, '-isysroot', sdkroot, min_version]
objcpp_args      = ['-arch', arch, '-isysroot', sdkroot, min_version, '-stdlib=libc++']
c_link_args      = ['-arch', arch, '-isysroot', sdkroot, min_version]
cpp_link_args    = ['-arch', arch, '-isysroot', sdkroot, min_version]
objc_link_args   = ['-arch', arch, '-isysroot', sdkroot, min_version]
objcpp_link_args = ['-arch', arch, '-isysroot', sdkroot, min_version]
